#include "fff.h"
#include <gtest/gtest.h>
/**
 * this file exists to provide any global definitions required by all tests.
 * rn its just the FFF macro. idk if this will be useful for anything else
 */

// This macro must be called exactly 1 time in the entire test suite
DEFINE_FFF_GLOBALS;
